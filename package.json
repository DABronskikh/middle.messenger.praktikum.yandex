{
  "name": "messenger",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "^20.9.0"
},
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview --open",
    "start": "npm run build && npm run server"
  },
  "devDependencies": {
    "sass": "^1.69.5",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "handlebars": "^4.7.8",
    "normalize.css": "^8.0.1",
    "vite-plugin-handlebars": "^1.6.0"
  }
}
